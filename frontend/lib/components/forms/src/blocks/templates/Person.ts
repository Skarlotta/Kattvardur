import { Field, Fields } from "../../lib/Fields";

export const Person: Field[] = [
    Fields.name,
    Fields.ssn,
    Fields.address,
    Fields.postcode,
    Fields.country,
    Fields.telephone,
    Fields.email,
]